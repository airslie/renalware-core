$(document).ready(function() {
  // display_configuration must be defined in audits#show before hand
  if (typeof display_configuration !== 'undefined') {
    $("body.audits_show table#audit").DataTable(display_configuration);
  }
});
