.row
  %h5 Edit Clinic

.row
  .large-12.columns
    = form_for [@patient, @clinic], url: patient_clinic_path(patient_id: @patient.to_param, id: @clinic.to_param) do |f|
      = render partial: 'form', locals: { f: f }
      = f.submit 'Update', class: 'button save'
