- if current_user.has_role?(:super_admin)
  %ul.right
    %li.has-dropdown
      = link_to 'Manage users', admin_users_path, title: 'Manage users'
      %ul.dropdown
        %li
          = link_to 'Approve users', admin_users_path(approved: false)
