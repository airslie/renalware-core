%nav.top-bar(data-topbar role = "navigation")
  %ul.title-area
    %li.name.has-dropdown.active
      %h1
        = link_to "RW2.0", patients_path, :title => "Home"

  - if user_signed_in?
    %section.top-bar-section
      = render partial: 'navigation/user_admin'
      = render partial: 'navigation/patient_admin'
      = render partial: 'navigation/patient_search'
      = render partial: 'navigation/sign_out'
      = render partial: 'navigation/help_items'
      = render partial: 'navigation/patients_admin'
      = render partial: 'navigation/renalware_admin'
