= render "record_header"

.row
  .columns.large-12
    .document
      = simple_form_for @donor_operation,
          url: patient_transplants_donor_operations_path(@patient),
          wrapper: "horizontal_form" do |f|
        = render 'form', f: f
