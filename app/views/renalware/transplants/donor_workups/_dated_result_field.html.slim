= f.simple_fields_for attribute, f.object.send(attribute) do |fd|
  tr.dated-confirmation
    td
      = fd.label :result, f.object.class.human_attribute_name(attribute)
    td
      = fd.input :result, label: false, wrapper: "vertical_form"
    td
      = fd.input :date, as: :date_picker, label: false, wrapper: "vertical_form"
