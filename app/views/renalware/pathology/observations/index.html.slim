= render layout: "renalware/patients/layout", locals: { title: "Archived Pathology Results" } do
  table#observations
    - rows.each do |row|
      - header, *values = row
      tr class= header.downcase
        td
          = header
        - values.each do |cell|
          td= cell
