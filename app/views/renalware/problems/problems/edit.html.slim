= within_patient_layout(title: "Edit",
                        breadcrumbs: [problems_breadcrumb(patient),
                                      problem_breadcrumb(patient, problem)])

  = render "form", problem: problem, patient: patient, url: patient_problem_path(patient, problem)
