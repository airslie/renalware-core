= content_for(:tabs) do
  = render "renalware/messaging/shared/messages_type_tabs", active: :read

= content_for(:filters) do
  = render search_form

= within_admin_layout(title: "Messages") do
  .read-messages
    = render "table", receipts: receipts
  = paginate receipts
