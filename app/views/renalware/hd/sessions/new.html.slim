= render layout: "renalware/patients/layout", locals: { title: "HD Session" } do
  = render "session_nav"

  .document
    = simple_form_for @session,
        url: patient_hd_sessions_path(@patient),
        wrapper: "horizontal_form" do |f|
      = render 'form', f: f
