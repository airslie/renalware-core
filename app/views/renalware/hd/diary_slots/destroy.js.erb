$("td[data-slot-id='<%= destroyed_slot_id %>']").replaceWith("<%= escape_javascript(
  render "slot", slot: replacement_slot, diary: diary, animate: true
) %>");

$(".reveal-modal").foundation('reveal', 'close');
