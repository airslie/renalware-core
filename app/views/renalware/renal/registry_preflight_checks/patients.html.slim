= content_for(:filters) do
  = render "filters",
           query: query,
           url: patients_renal_registry_preflight_checks_path

= content_for(:tabs) do
  = render "navigation"

= within_admin_layout(title: "Renal Registry Return: Preflight Checks") do
  = render "table", patients: patients, query: query, pagy: pagy
