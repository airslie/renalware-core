.row
  = render "header"

.row
  .large-6.columns
    = form_for @doctor, as: :doctor do |f|
      = render partial: 'form', locals: { f: f }
      = f.submit 'Save', class: 'button'
      span= " or "
      = link_to "cancel", doctors_path
