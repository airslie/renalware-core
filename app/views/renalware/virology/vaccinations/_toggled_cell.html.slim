/ See Vaccination.to_partial_path etc
- document = event.document

dl.dl-horizontal
  dt= attr_name(document, :type, suffix: ":")
  dd= document.type&.text || t("unspecified")
  dt= attr_name(event, :notes, suffix: ":")
  dd= event.notes || t("unspecified")
