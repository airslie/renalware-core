.select-exit-site-drug
  = pm.label :medicatable_id, "Select Drug (exit site drugs only)"
  = pm.collection_select :medicatable_id, Renalware::Drug.peritonitis, :id, :name, { prompt: "Please select drug used to treat this exit site infection" }, class: "exit-site-drug-select"

  = pm.hidden_field :medicatable_type, value: "Renalware::Drug", class: "selected-medicatable-type"
