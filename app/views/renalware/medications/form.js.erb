$("#medications").html("<%= escape_javascript(
  render "form", medication: medication,
                 patient: patient,
                 treatable: treatable,
                 url: url
) %>");

initDatepickersIn("#medications");
