$("#form").html("<%= escape_javascript(
  render "form",
        patient: patient,
        available_primary_care_physicians: available_primary_care_physicians
) %>");
$("#form .searchable_select").select2();
