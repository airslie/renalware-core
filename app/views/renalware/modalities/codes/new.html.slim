= render "header"

.row
  = render partial: "renalware/shared/errors", locals: { model: @modal_code }

.row
  .large-6.columns
      = form_for @modal_code, method: :post do |m|
        = render partial: "form", locals: { m: m }

        = submit_tag "Save", class: "button"
        span= " or "
        = link_to "cancel", modalities_codes_path
