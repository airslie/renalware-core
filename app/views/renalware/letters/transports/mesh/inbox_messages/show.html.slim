table
  tr
    th(style='vertical-align: top') Payload
    td
      - if response.headers["content-type"] != "application/octet-stream"
        b Non-XML content!
      - else
        pre= Nokogiri::XML(response.body)
