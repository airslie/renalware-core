- if @exit_site_infection.try(:new_record?)
  = render "renalware/pd/dashboards/navigation"
- else
  dl.sub-nav
    dd= link_to "PD Summary", patient_pd_dashboard_path(@patient)
    dd= link_to "View Exit Site Infection", patient_pd_exit_site_infection_path(@patient, @exit_site_infection)
