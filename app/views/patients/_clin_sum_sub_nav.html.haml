%dt  
  %dd.remove-name.patient-name{role: "menuitem"}
    = link_to "#{@patient.full_name}", clinical_summary_patient_path(@patient), :title => "Clinical Summary", :"data-tooltip aria-haspopup" => true
  %dd{role: "menuitem"}
    = link_to "Letters", "#", :title => "View Letters", :title => "Letters"
  %dd{role: "menuitem"}
    = link_to "Admin", demographics_patient_path(@patient), :title => "Demographics"
  %dd{role: "menuitem"}
    = link_to "Modal", modality_patient_path(@patient), :title => "Patient Modality"
  %dd{role: "menuitem"}
    = link_to "Problems (#{@patient.problems.length})", problems_patient_path(@patient), :title => "Manage Problems"
  %dd{role: "menuitem"}
    = link_to "Meds (#{@patient.patient_medications.length})", medications_patient_path(@patient), :title => "Manage Medications"
  %dd{role: "menuitem"}
    = link_to "Events (#{@patient.patient_events.length})", patient_patient_events_path(@patient), :title => "Patient Events"


