.wide-screen
  .columns.large-12
    = render partial: "patients/patient_info_header", locals: { model: @patient }
    = render partial: "patients/mini_renal_profile", locals: { model: @patient }

    %h5 PD Info
    %p  
      = link_to "Add Peritonitis Episode", new_patient_peritonitis_episode_path(@patient), :class => "button tiny" 
      = link_to "Add Exit Site Infection", new_patient_exit_site_infection_path(@patient), :class => "button tiny"
.wide-screen
  %h6
    = "Peritonitis Episodes (#{@peritonitis_episodes.length})"
                                
  = render partial: "patients/peritonitis_table"

  %h6
    = "Exit Site Infections (#{@exit_site_infections.length})"

  = render partial: "patients/exit_site_table"
                                  