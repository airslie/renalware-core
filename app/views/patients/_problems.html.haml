%thead
  %th
    Problem
  %th
    Entered By
  %th
    Date

%tbody
  - @patient.problems.each do |p|   
    %tr{:class => "problem"}
      %td{:data => { :heading => "Problem" }}
        = p.description 
      %td{:data => { :heading => "Date" }}
        = p.user_id
      %td{:data => { :heading => "Date" }}
        = p.created_at.strftime("%d/%m/%Y")
      
      