.esrf-info-form
  = esrf.hidden_field :id

  = esrf.hidden_field :patient_id

  %li
    = esrf.label :date, "Date"
    = esrf.date_select(:date, {start_year: (120.years.ago).year, end_year: Time.now.year, order: [:day, :month, :year]}, class: "datetime")

  %li
    = esrf.label :prd_code_id, "Renal Diagnosis"
    = esrf.collection_select :prd_code_id, PrdCode.all, :id, :term, { :prompt => "Select renal diagnosis", include_blank: true }, :class => "prd-select"
