#new-form
  .large-12.columns
    %fieldset
      %legend
        Add a new medication

      .large-12.columns
        = pm.link_to_remove "Remove this medication", class: "button cancel tiny right remove-bag hide"

        = render partial: 'shared/error_messages', locals: { pm: pm }

        .row
          = render partial: 'medications/fields', locals: { pm: pm }

